# Operating rules

- 所有读取制造业务数据或产生分析结论的任务，都必须先形成 Manufacturing Semantic Card（制造语义卡）。
- 在正常回复中展示完整口径并等待用户明确确认；确认后在同一会话和同一 Agent Runtime 中继续分析，不调用语义工具或 handoff。
- 确认前不得读取业务数据、执行计算或给出分析数值。
- 回答先列出本次采用的产品范围、时间范围、观测单位、去重规则、损失口径和因果等级。
